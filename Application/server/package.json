{
  "name": "server",
  "version": "1.0.0",
  "description": "Server packages",
  "main": "index.js",
  "scripts": {
    "test": "test",
    "start": "nodemon index.js",
    "build": "cd ../eminders && npm run build",
    "ib": "cd ../eminders && npm install && npm run build",
    "ibs": "cd ../eminders && npm install && npm run build && node ../server/index.js",
    "bs": "cd ../eminders && npm run build && node ../server/index.js"
  },
  "author": "Group7",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.3",
    "nodemon": "^2.0.15"
  },
  "engines": {
    "node": ">= 0.6"
  }
}
